*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/release/output-metadata.json
/.kotlin/

# Claude Code local settings (YOLO/bypassPermissions — không commit)
.claude/settings.local.json

# Signing credentials — nay lấy trực tiếp từ repo private royt93/myKeyStore, không giữ
# bản copy trong repo này nữa (xem app/build.gradle.kts)
keystore.properties
*.jks

# Ad network config (App ID/ad-unit ID thật) — lưu ở royt93/myKeyStore/com.samsunggalaxy.bmicalculator
# thay vì commit vào repo public này (bị scraper bot dò ca-app-pub-... trước khi app launch).
doc/admob/
